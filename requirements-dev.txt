# Test-only dependencies — not needed to run the app, only to run
# `python -m pytest`. Install with:
#     pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
httpx==0.27.2
